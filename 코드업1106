#include <stdio.h>
int main() 
{
 	printf("%d %d", 0x80000000, 0x7fffffff);
 	return 0;
}

참고)
대입문을 사용할 때 8진수는 0을 앞에 붙여서 입력하고,
16진수는 0x를 붙여서 입력합니다.
예)
a = 0xff;     //a에 16진수 ff를 넣음. ff는 십진수로 255입니다.
