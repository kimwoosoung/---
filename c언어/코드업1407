#include<stdio.h>
 
 
int main(){
    
    char arr[101];
    
    gets(arr);
    
    for (int i = 0; arr[i] != '\0'; i++) {
        if (arr[i] != ' '){
            printf("%c", arr[i]);
        }
    }
    
    return 0;
  
    
 
}
