#include <stdio.h>

int main() {
    int h,m,n;
    scanf("%d %d",&h,&m);
    
    m -= 30;
    
    if(m<0){
        m=60+m;
        h--;
    if(h<0){
        h=23;
    }
    }
    printf("%d %d",h,m);
    return 0;
}
