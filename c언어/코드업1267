#include<stdio.h>

int main(){

    int num,num2,sum;
    scanf("%d",&num);
    sum = 0;
    for(int i=1; i<=num; i++){
        scanf("%d",&num2);
        if(num2%5==0)
        sum+=num2;

    }
    printf("%d",sum);
    return 0;

}
