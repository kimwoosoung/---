#include <stdio.h>

int fibo(int n){
    if(n>2){
        return fibo(n-1) + fibo(n-2);
    }
    else return 1;
    
}


int main() {
    int a;
    scanf("%d",&a);
    printf("%d",fibo(a));

    return 0;
}
