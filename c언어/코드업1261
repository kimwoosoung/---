#include <stdio.h>

int main() {
    int a[10];
    int b = 0;
    
    for (int i = 0; i < 10; i++) {
        scanf("%d", &a[i]);
    }

    for (int i = 0; i < 10; i++) {
        if (a[i] % 5 == 0) {
            printf("%d\n", a[i]);
            b = 1;
            break;
        }
    }

    if (!b) {
        printf("0\n");
    }

    return 0;
}
